# Part 16 — Overlay UX polish (IDEA-022)

Карточки TZ **№13–19** / шаги **P1–P9**. Код: `overlay_player.py`, проводка в `Subtitle_App.py`.

Команда: `делаем part 16` / `делаем P1` … `делаем P9`.

---

## P1 — Opacity каталога

**Done:** после stop/Esc/idle окно непрозрачно (`opacity=1`); слайдер прозрачности действует только во время play.

**Смоук:** play → прозрачность 40% → stop или Esc → каталог сплошной.

## P2 — Обычные окна

**Done:** нет `WindowStaysOnTopHint` / topmost у Фона; SR и Фон ведут себя как обычные окна.

**Смоук:** Cursor поверх Фона и SR.

## P3 — Назад

**Done:** кнопка «← Назад» закрывает overlay (`close` → `closed`); главное SR снова видно. Крестик — то же.

**Смоук:** Фон → Назад → SR на месте.

## P4 — Без «Обновить»

**Done:** кнопки ↻/Обновить нет; список обновляется при смене папки и `showEvent`.

## P5 — Levan Polka

**Done:** файл удалён из `Music\YouTube_DL` (если был).

## P6 — Настр. без зависания

**Done:** «Настр.»; перед диалогом play на паузе; открытие/закрытие без «Не отвечает».

## P7 — Плеер-хоткеи

**Done:** Space play/pause; ←/→ seek ±5с; ↑/↓ громкость; **Ctrl+O** = сквозь; **Ctrl+Shift+O** = скрыть/показать. Подсказка обновлена.

## P8 — Музыка в фоне

**Done:** hide / сквозь не стопают трек; хоткеи hide/сквозь работают скрыто если `play_when_hidden` (по умолчанию вкл); чекбокс в Настр.

## P9 — Карта хоткеев

**Done:** в Настр. поля хоткеев; persist `~/.subtitle_ripper/overlay_prefs.json`; переживает рестарт.

---

## Регрессия

- [ ] Фон открывается из SR, Назад возвращает
- [ ] Play + opacity + stop → непрозрачно
- [ ] Ctrl+O сквозь; Ctrl+Shift+O hide (музыка играет)
- [ ] Настр. не вешает процесс
