# MAP — YouTube_Translator

Канон статуса / очереди / плана. Техника — `docs/TZ.md` + `docs/parts/`.

| | |
|---|---|
| **Где мы** | Фон (`overlay_player.py`): клики кадр play/pause · 2× stage↔каталог ✅. Авто-плотность — **ТЗ готово, код ⬜** |
| **Фокус** | IDEA-025 / карточка №20 · part **18** · слои **A0→A1→A2** |
| **Код scope** | Только `overlay_player.py` (пока не скажешь иначе) |

---

## Очередь

| # | Что | Статус |
|---|-----|--------|
| 1 | MVP + ТЗ авто-плотности (этот MAP + part 18) | ✅ сейчас |
| 2 | **A0** — prefs + галка в Настр. | ⬜ следующий |
| 3 | **A1** — idle-таймер → сквозь + целевая плотность | ⬜ |
| 4 | **A2** — сброс / не мигать на смене трека | ⬜ |
| — | SMTC vs Chrome (наушники) | ⏸ парковка |
| — | IDEA-021 файлы dist/ | ⏸ парковка |

---

## План следующего куска: A0

Команда: **`делаем A0`** / **`делаем part 18`** (A0 только).

1. Prefs: `auto_density` (bool), `auto_density_pct` (int), `auto_density_idle_sec` (int).
2. Настр. Фон: галка «Авто-плотность» + спиннеры % и сек (дефолты из part 18).
3. Пока **без** таймера поведения — только сохранить/загрузить prefs.
4. Смоук: открыл Настр. → вкл → ок → переоткрыл → значения на месте.

Не склеивать A1 в тот же diff.

---

## Запреты

- Не трогать `Subtitle_App.py` / SMTC / список треков без запроса.
- Плотность `<100%` **только со сквозь** (текущий контракт `_apply_stage_opacity`) — авто не ломает это: сначала CT, потом %.
- Каталог остаётся 100% opacity.
- Не auto-ИИ, не IDEA-021, не полный рефактор overlay.
- Пуш только по просьбе.

---

## MVP одной фразой

В **полном окне**, пока **играет**: N сек без мыши по кадру → сам **сквозь + плотность X%**. Движение / Esc / выкл сквозь → выйти из авто-режима; музыка не стопается.
