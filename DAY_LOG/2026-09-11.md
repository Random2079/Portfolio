# 2026-09-11

## YouTube_Translator / overlay «Фон»
- Убрал второе окно `Фон — панель` (chrome-host): кнопки остаются в том же HWND, что и видео.
- Повторный запуск overlay: если окно уже есть — поднимаем его, не плодим процесс.
- Ctrl+O прячет управление (только видео). Полный экран снова topmost (`WS_EX_TOPMOST` + флаг Qt).
- Стабилизация 1 окно: Ctrl+Shift+O только hide (без Translator); Ctrl+O = UI hide + плотность 40%; debug-лог выкл (OneDrive freeze); меньше timer/repair.
